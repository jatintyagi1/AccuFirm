import { createContext } from "react";

// Create a UserContext with a default value of null
const UserContext = createContext(null);

// export const UserContext = () => ({ Provider, Consumer } = createContext());


export default UserContext;
